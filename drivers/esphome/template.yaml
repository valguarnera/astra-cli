esphome:
  name: {{NODE_ID}}
  friendly_name: {{NODE_FRIENDLY}}

esp8266:
  board: {{BOARD}}

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  power_save_mode: none
  reboot_timeout: 0s

mqtt:
  broker: "{{MQTT_HOST}}"

logger:

api:

ota:
  - platform: esphome

web_server: